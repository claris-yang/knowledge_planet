//
// Created by yangtao on 20-5-20.
//
#include <iostream>
#include <cstring>
#include <csdio>
#include <algorithm>
using namespace std;
const int N = 1005;
int d[N][N];
int n, m;
int main() {
    cin >> n >> m;
    for(int i = 0; i < m; i++) {
        int x1, y1, x2, y2;
        cin >> x1 >> y1 >> x2 >> y2;
        d[x1][y1] += 1;
        d[]
    }
}